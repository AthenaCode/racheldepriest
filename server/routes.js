'use strict'

const express = require('express');
const router = new express.Router();

router.get('/', (res, req) => {
	
})

router.get('/aboutMe', => {

})

router.get('/skills', => {

})

router.get('projects' => {
	
})


module.export = router;